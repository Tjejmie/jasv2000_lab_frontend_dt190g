/**
 * Represents all MIUN-courses
 */
export interface course {
  _id: string,
  courseCode : string,
  subjectCode: string,
  level: string,
  progression: string, 
  name: string, 
  points: number, 
  institutionCode: string,
  subject: string, 
  }
  