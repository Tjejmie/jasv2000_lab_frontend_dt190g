/**
 * Represents all MIUN-courses
 */
export interface courses {
    courseCode: string,
    subjectCode: string,
    level: string,
    progression: string, 
    name: string, 
    points: Number, 
    institutionCode: string,
    subject: string, 
  }
  